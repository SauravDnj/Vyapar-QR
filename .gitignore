node_modules
.next
.turbo
dist
.env
.env.local
.env.*.local
.env.*.bak*
.DS_Store
*.log
*.tsbuildinfo
next-env.d.ts
apps/api/uploads
dump.rdb
.tools
.claude/settings.local.json

# Local JSON database (the `local` JSONDB_DRIVER writes one file per model).
# Real data, not fixtures — seed it with `pnpm --filter api db:seed`.
apps/api/data
